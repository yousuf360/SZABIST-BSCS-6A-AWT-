package edu.springmvc.hiber.sevice;


import java.util.Date;
import java.util.List;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/*import edu.springmvc.hiber.bean.Book;
import edu.springmvc.hiber.bean.Issue;
import edu.springmvc.hiber.bean.Member;*/




//@Service
//@Transactional
//public class StudentService extends BaseService{
//	@SuppressWarnings("unchecked")
//	public List<Student> ListStudents(){
//		return getSession().createQuery("from Student").list();
//	}

/*	@SuppressWarnings("unused")
	public void doIssue(){
		Book book = (Book)getSession().get(Book.class, new Integer(100));
		Member member = (Member) getSession().get(Member.class, new Integer(20));
		
		Issue issue = new Issue();
		issue.setBook(book);
		issue.setMember(member);
		issue.setIssuedate(new Date());
		
		getSession().save(issue);
		
	}*/
	
	
	
	/*public void AddStudent(Student student){
		getSession().persist(student);
	}*/
	
/*	public void AddPostandAuthor(){
		Post post = new Post("One to One Mapping");
		Author author = new Author("David");
		
		post.getComments().add(new Comments("Spring MVC"));
		post.getComments().add(new Comments("With Hibername"));
		
		author.setPost(post);
		
		getSession().save(post);
		getSession().save(author);
	}*/
	

